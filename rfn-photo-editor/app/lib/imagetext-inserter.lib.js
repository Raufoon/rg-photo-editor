function ringImageTextInserter(angularScope, mainCanvasId) {
    var mainCanvas,
        textArea,
        fontStyle,
        fontColor,
        fontSize;

    this.init = init;
    this.exit = exit;
    this.addtext = addtext;

    function init() {
        mainCanvas = document.getElementById(mainCanvasId);
    }
    
    function exit() {
        
    }
    
    function addtext() {

    }
}